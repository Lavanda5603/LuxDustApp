@{
    ViewData["Title"] = "Результаты подбора";
}

<h2>Ваша персональная подборка готова!</h2>

@if (Model != null && Model.Any())
{
    <p>Первый тестовый продукт: <strong>@Model[0].Name</strong></p>
}
else
{
    <p>-/p>
}